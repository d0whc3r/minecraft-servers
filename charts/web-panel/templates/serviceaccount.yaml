apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "web-panel.serviceAccountName" . }}
  labels:
    {{- include "web-panel.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
